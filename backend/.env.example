PORT=5000
JWT_SECRET=your_jwt_secret_key_change_this_in_production
NODE_ENV=development

# Database Type: mysql or dynamodb
DB_TYPE=mysql

# MySQL Configuration (for XAMPP - default)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=food_ordering
DB_PORT=3306

# AWS Configuration (only needed if DB_TYPE=dynamodb)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# DynamoDB Table Names (only needed if DB_TYPE=dynamodb)
# DYNAMODB_USERS_TABLE=FoodOrdering-Users
# DYNAMODB_MENU_TABLE=FoodOrdering-MenuItems
# DYNAMODB_ORDERS_TABLE=FoodOrdering-Orders
